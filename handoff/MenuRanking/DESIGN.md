# MenuRanking — 値の一覧（App-MenuRanking-Dark.html から抽出・@393 基準）

一次情報は `App-MenuRanking-Dark.html`（1 枚の phone でプロフィール → 店を選ぶ → 編集 → メニュー選択シート → 詳細を操作できる）。
ver 7.0.0 の要望は `PLAN-ver7.md`（タブ無し = ランキングのタイムラインだけ / 場所検索は DB のみ・直径 100m / Google マップのリンクで追加）。

## 共通（dark）
- 面: bg `#000` / sheet `#0e0e12` / raise `#16161b` / fg `#fff` / fg2 `rgba(255,255,255,.58)` / fg3 `.36` / line `.10` / band `.06`
- 写真の地: `#1a1a20`。白縁の写真 = border 3〜4 `#fff` + 影 `0 4px 14px rgba(0,0,0,.6)`
- 序数 = Inter 900 italic（`1st` の `st` は .68em）。和文は Noto Sans JP、英字名は Inter

## A. 一覧（プロフィール = ランキングのタイムライン）
- 1 本 = `.rk-stage` 高さ 472 = pad 14×2 + cover 232 + 下 212。下に band 8
- cover: left/right 2・top 20・高さ 232・border 4 白・rotate ±2.5°（seed）・上 60% にスクリム .5 → 0・右下に `@ 店名` の pill（白 .72 blur・11.5 w800・r13・h26）
- 3 枚: 列幅 cw = (W−12)/3、中心 cx = 6 + cw·j + cw/2、top 268、142×189・border 3 白・rotate ±10°・dx ±6・z 30/28/26
- 名前の帯: 写真の上（top 256）、白地・pad 4 7 5 6・影・rotate ±9°・max-width min(cw+4, 128)・序数 13 + 名前 w800 12（長ければ 10）2 行
- 見出し `.pk-tout`: left 28 top 28・rotate −3°・l1 = タイトル 22 w900 白 + 黒 stroke 5 / l2 = `BEST N` Inter italic 900 24
- 空: 「まだランキングがありません」+「お店のメニューに順位をつけて並べられます。右下の ＋ から、いまいるお店を選んで作りましょう。」
- FAB: 55・面なし・add 28 白 + drop-shadow

## B1. お店を選ぶ（`#st`）
- 左上 × (pk-back)・見出し「お店を選ぶ」16 w700 + 「現在地から半径 10m 以内」11.5 fg2（ver7 = 直径 100m）
- 位置許可の面: アイコン丸 72（白 .08）+ 見出し 20 w800 + 本文 13.5 fg2 + CTA 56 白「位置情報をオンにする」→ iOS ダイアログ。拒否 = 「設定を開く」+ 「もう一度許可をリクエスト」
- 検索中: スピナー 28 + 「半径 10m 以内のお店を探しています」
- 一覧: eyebrow「見つかったお店 N」→ カード `.st-card` = `.pk-cover` 4:3（写真 + 上 48% / 下 58% のスクリム）+ 店名 20 w800（左上・2 行）+ ジャンル pill（右上・gc 色・h24 11）+ 左下 ★評価 / 営業中 + 時間 / 住所 + 右下 距離 pill（白 .95・Inter 700 12）。間 8
- 末尾: 「見つからないときは、お店の中に入ってからもう一度探してください。」+ ボタン「もう一度探す」

## B2. 編集（`.rd.me-ed`）
- ヘッダー（黒 → 透明のグラデ・高さ safe+104）: 戻る 44 / タイトル入力 22 w900 中央・下線 1.5 白 .4（focus 白）・placeholder「タイトルを入力」・max 20 / 下に `@ 店名` 12 fg2
- スクロール pad-top safe+112・下 130。cover 16:9・border 3 白・rotate −1.5°・左下「変更」pill（黒 .5 + 白 .7 の内縁・h32）
- 行 `.rd-row`: grid 1fr | 62%（奇数行は反転）・行の重なり −56・pad 0 14・写真 9:16 border 3 白 rotate ±6° dx ±6・文字は中央揃え = 序数 56（small 18）+ 名前 17 w800
- 空の行: 破線 2px 白 .34・地 `#0c0c10`・「メニューを選ぶ」+ add 26・文字「タップして選ぶ」14 fg3
- 埋まった行: 右上 `.ph-cam` 36 丸（黒 .5 + 白 .6 内縁）= 写真を差し替え / 左下「オリジナル」pill（自分の写真のとき）
- 末尾 `.ed-add`: 破線 pill 52・「N位を追加」（10 まで）。保存 = `pk-save`（空なら .35）→ スピナー → 一覧の先頭へ
- タイトル空の既定 = 「{店名} のおすすめ」

## メニュー選択シート（`.me-sheet` --sh 640・r24・dim .62）
- 箱 `.pk-cols` 横並び: `.pk-colm` = mini 54×72 写真 + 順位ステッカー（top 70・rotate −6°）+ × + 名前 11.5。空 = 破線・次に入る枡は縁 .75 + 「次に入る」
- `.me-bar`: 案内（「メニューにチェックすると順に入ります」/ 2 件以上「ドラッグで順位を入れ替え」）+ 「決定」（h40・黒 .45 + 白 .75 内縁）
- リスト: eyebrow「メニュー N」+ 3 列 `.pk-g2`（pic 3:4・r10・チェック 24 右上 −10・順位ステッカー・名前 13）。先頭は「商品を追加」（破線）。空文「このお店のメニューはまだ登録されていません。最初の商品を追加してください。」
- 上限 10 件。並べ替えは横ドラッグ（86px = 1 列）

## 商品を追加（`.am`）
- raise 面・r24・見出し「商品を追加」+ ×・「商品名」入力 48（placeholder 例：カフェラテ・max 24）・「写真 / カメラで撮影」= 72×96 のタイル（破線「カメラ」→ 撮り直す / 撮った 1 枚 = 選択 + tag「自分」）・CTA 52 白「追加する」（名前 + 写真が揃うまで .35）

## D. 詳細（`.rd`）
- ヘッダー safe+96（黒 → 透明）: 戻る + `.pk-tout` 中央（l1 24 / l2 26・stroke 5）
- cover 16:9 rotate ±5°（seed）+ `@ 店名` tag → 行は編集と同じ（読み取り専用）・pad 下 64
